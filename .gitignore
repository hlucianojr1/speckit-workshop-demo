# Local build + dependency outputs.
build/
build-*/
out/
vcpkg_installed/
compile_commands.json
.cache/
CMakeUserPresets.json

# IDE per-user.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Local crash-dump working copies (committed fixtures use the .dmp / .pdb without
# the .local suffix; the .local variants are gitignored).
*.dmp.local
*.pdb.local
*.core.local

# Compiled debug info from capture scripts (capture.ps1 / capture.sh).
*.pdb

# Logs.
*.log
#copilot local indexes, parsed segments and auxiliary files SQLite database files.
*.db-shm
*.db-wal
**/CopilotLocalIndex/

#VS Code
.vs/
/rust-port
rust-port/*