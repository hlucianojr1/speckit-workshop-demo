# Spec-Kit specs

This directory holds the Spec-Kit constitution and per-feature specifications. Two demo
features are pre-staged (skeleton only — `/speckit.specify`, `/speckit.plan`,
`/speckit.tasks`, `/speckit.implement` are performed live during Part 2 of the workshop):

- [`lockless-ring-buffer/`](lockless-ring-buffer/) — Part 2, Feature 2 (§2.3 of the training).
- [`fixed-string/`](fixed-string/) — Part 2, Feature 3 (§2.4 of the training).

Ground truth: [`constitution.md`](constitution.md) (machine-readable copy:
[`.specify/memory/constitution.md`](../.specify/memory/constitution.md)).
