# Spec-Kit specs

This directory holds the Spec-Kit constitution and per-feature specifications. Two demo
features are pre-staged (skeleton only — `/specify`, `/plan`, `/tasks`, `/implement` are
performed live during Sessions 06 and 07):

- [`lockless-ring-buffer/`](lockless-ring-buffer/) — Session 06 (Spec-Kit Mastery Pt 1).
- [`fixed-string/`](fixed-string/) — Session 07 (Spec-Kit Mastery Pt 2).

Ground truth: [`constitution.md`](constitution.md).
