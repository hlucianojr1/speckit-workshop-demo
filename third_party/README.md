# third_party/

Vendored or submoduled dependencies. Build outputs here are gitignored.

`EASTL.commit` records the pinned EASTL revision. To update, open a PR with rationale and
re-run the CI snippet validator + clang-tidy gates.
